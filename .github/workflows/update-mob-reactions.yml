name: Update Mob Reactions

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Mob Reactions version to update to (leave empty for latest)'
        required: false
        default: ''
  repository_dispatch:
    types:
      - mob-reactions-updated

jobs:
  update:
    name: Update Mob Reactions in modpack
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install packwiz
        run: |
          go install github.com/packwiz/packwiz@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Update mob-reactions
        run: packwiz update mob-reactions

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update mob-reactions mod\n\nCo-Authored-By: Paperclip <noreply@paperclip.ing>"
          branch: main
