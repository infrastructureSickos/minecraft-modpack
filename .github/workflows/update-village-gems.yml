name: Update Village Gems

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Village Gems version to update to (leave empty for latest)'
        required: false
        default: ''
  repository_dispatch:
    types:
      - village-gems-updated

jobs:
  update:
    name: Update Village Gems in modpack
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install packwiz
        run: |
          go install github.com/packwiz/packwiz@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Update village-gems
        run: packwiz update village-gems

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update village-gems mod\n\nCo-Authored-By: Paperclip <noreply@paperclip.ing>"
          branch: main
